# Proj_0801 深度学习项目迁移说明

2026-08-01 起，直接读取 MRI 与椎旁肌分割 NIfTI 的 2.5D 深度学习试验位于独立路径 `E:\code\Proj_0801`。`Proj_0501` 保持手工 MRI 特征与临床变量建模主线，不包含或调用深度学习包。

两条路线只共享同一权威标签来源：`data/labels/PATIENT_LIST_FILE.csv`。新项目通过自己的迁移脚本生成仅含 `patient_id`、`instability_label` 和 `image_file` 的本地最小副本；源表仍由本项目维护且不会被覆盖。新项目的训练、缓存、清单、折划分和输出均写入自己的目录。

当前 `Proj_0501` 内没有 312 例 MRI/分割 NIfTI 本体，因此这些大文件不属于本次目录迁移范围，也不能从本项目自动补齐。正式深度学习训练前需在 `Proj_0801` 中另行配置真实 NIfTI 数据源并完成逐例审计。
