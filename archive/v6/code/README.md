# v6 archived code

This directory is an immutable provenance snapshot of the Pearson-screened
six-factor experiment.

The original entry point was `run_pearson_factor_replication.py`; it is stored
here as `run_experiment.py`. The script was executed with:

```text
C:\ProgramData\anaconda3\envs\nnUNet-master\python.exe
```

The archived result is under:

```text
archive/v6/results/v6_pearson_factor_replication
```

The code keeps the original project-relative input paths and original live
output directory. To reproduce without overwriting a live result, copy the
snapshot to a separate working directory and edit `OUTPUT_DIR`.

